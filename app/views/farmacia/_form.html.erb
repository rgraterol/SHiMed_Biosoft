<%= form_for(@farmacium) do |f| %>
  <% if @farmacium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@farmacium.errors.count, "error") %> prohibited this farmacium from being saved:</h2>

      <ul>
      <% @farmacium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reg_sanitario %><br>
    <%= f.text_field :reg_sanitario %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
